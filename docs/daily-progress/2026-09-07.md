# DAP Daily Progress — 2026-09-07

## Summary

Substantive DAP v2.2 work continued on the isolated `v2.2/dual-entry-user-mode` branch. The day established the Local AI timeout root cause, froze the first User Mode authentication surface, implemented the first controlled authentication foundation in a still-uncommitted worktree, and then stopped fail-closed when an application route-contract verifier disagreed with the router/OpenAPI evidence.

No unfinished v2.2 feature implementation is included in this documentation change.

## Verified progress

### P1.1.F0 — Qwen explicit-route timeout forensics

- Source/release continuity remained sealed at HEAD `73811d151b41925e6acede6702191b2681d6c85c` and tree `799f12193d77622d508cfd8f6a81b849ee20faa1`.
- Both Local AI services remained active with zero restarts and listeners bound to localhost.
- The failed Qwen probe was classified from captured evidence without retrying inference.
- Qwen successfully loaded and generated 919 completion tokens at about 6.71 tokens/second; total server processing was about 142.9 seconds after the model load, exceeding the probe's 120-second client timeout.
- The front router later attempted to return HTTP 200 and encountered `BrokenPipeError` only because the client had already disconnected.
- No OOM/resource event, model corruption, service crash, source mutation, database write, Docker/service/firewall/Cloudflare/DNS action occurred.

### P1.1.R1 — bounded Qwen recovery proof

- The bounded explicit Qwen cold-switch proof passed.
- Granite primer: HTTP 200 in about 5.36 seconds.
- Explicit Qwen route: HTTP 200 in about 16.76 seconds.
- Qwen usage was real and bounded: 176 prompt tokens, 9 completion tokens, 185 total tokens, `finish_reason=stop`.
- Explicit route metadata remained `qwen-coder`, route reason `explicit`, safety `advisory-only`, mutation `none`.
- The recovery classification set the root cause to an unbounded completion rather than a Qwen/model/router failure.
- Initial public completion-cap requirement was frozen at 384 tokens; browser clients are not intended to raise that server-side ceiling.

### P1.2 — exact P2 implementation-surface freeze

- P1.2 passed with a clean worktree before implementation.
- The P2 source boundary was frozen to exactly 11 files:
  - `platform/backend/app.py`
  - `platform/backend/requirements.txt`
  - eight `platform/backend/user_mode/` modules
  - `platform/backend/tests/test_user_mode_auth.py`
- P2 scope was restricted to authentication and isolated User Mode persistence only.
- Password hashing was frozen to Argon2id via `argon2-cffi`.
- Session design was frozen to opaque server-side sessions with hashed tokens at rest, `HttpOnly`/`Secure`/`SameSite=Lax` cookie behavior, 24-hour idle timeout, and 7-day absolute timeout.
- Initial account ceiling was frozen at two total accounts and one admin account; public registration/signup remained closed.
- Production target database `/home/dipen/dap/user-mode-data/user-mode.db` remained absent and its creation was not authorized in P2.
- Local AI, Career, Knowledge, Dashboard, User Portal, Docker, service, firewall, Cloudflare, DNS, and public-exposure changes were explicitly outside P2.

### P2 — first controlled authentication implementation, stopped fail-closed

- The implementation stayed inside the frozen 11-file surface.
- `argon2-cffi 25.1.0` was installed in the backend test virtual environment for the bounded test run.
- Python compilation passed.
- Focused User Mode authentication regression: **16 passed**.
- Existing Local AI regression: **14 passed**.
- The implementation then stopped at the application route-contract check before any commit was created.
- The failing verifier observed an empty method set for `/api/v1/user/auth/login` while expecting `POST`.
- Because the P2 driver failed, the backend was not restarted, no production User Mode database was created, P3 UI work was not started, and no P2 source commit was created.

### P2.F0 — route-contract failure forensics

- The partial worktree was proven to contain exactly the frozen 11 P2 files and no unexpected paths.
- Raw `user_mode` router objects were correct:
  - login → `POST`
  - session → `GET`
  - logout → `POST`
- Imported `app.routes` showed zero matching User Mode route objects, reproducing the failing checker behavior.
- Independent OpenAPI generation nevertheless contained all three expected paths/methods.
- Duplicate-route checks found zero duplicate target paths.
- Production User Mode DB remained absent after app import.
- Local AI router SHA remained unchanged.
- F0 made no source edit, commit, database write, service, Docker, firewall, Cloudflare, or DNS action.
- Final classification remained `PARENT_P2|FAIL` with the 11-file partial worktree intentionally preserved for the smallest follow-up repair/verification.

## Safety and release boundary

- DAP v2.1 release continuity remained unchanged during the verified work.
- Local AI v1 remained immutable.
- No production User Mode database exists from this work.
- No production backend restart or public activation occurred.
- No unfinished v2.2 feature code is merged into `main` by this documentation PR.
- P2 is **not** recorded as complete; the route-verifier/application-wiring anomaly remains an open fail-closed boundary.
- No DAP phase gate, execution authority, Guardian policy, Career authority, Telegram approval boundary, Cloudflare/DNS exposure, or auto-apply/application-submission authority was weakened or expanded.

## Next verified boundary

The next engineering step is to classify the checker-versus-application wiring discrepancy using in-memory dispatch/OpenAPI evidence and then apply only the smallest justified P2 repair or verifier correction before any P2 commit or activation.
